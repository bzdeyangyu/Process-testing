# Project Claude Guide

- Prefer `skill_registry.invoke()` for specialist calls
- MVP first: `req_parser`, `material_style`, `visual_prompt`
- Real LLM backend: GLM
- Use evaluation before broad mechanism expansion
